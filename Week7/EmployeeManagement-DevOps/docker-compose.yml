services:

  backend:
    build:
      context: ./employee-backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./react-app
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    depends_on:
      - backend